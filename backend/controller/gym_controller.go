package controller

/*
import (
	"strconv"
	"net/http"
	"github.com/gin-gonic/gin"
)

func GetHotel(context *gin.Context) {
	HotelIdString := context.Param("id")
	HotelId, err := strconv.Atoi(HotelIdString)
	if err != nil {
		context.String(http.StatusBadRequest, "ID invalido")
		return
	}
	context.JSON(http.StatusOK, )
}
*/
